# The project's domain

`todo-cli.example`, registered at Namecheap for $2.00, paid with a single-use card minted through the Open
Autonomy card rail (`POST /v1/rails/card`, purpose "domain todo-cli.example"). The purchase is on the
project's audit trail and on the `domain` item's page.
