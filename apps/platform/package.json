{
  "name": "@open-autonomy/platform",
  "version": "2.0.0",
  "description": "Open Autonomy's public platform: the treasury with patronage on top \u2014 money in from GitHub Sponsors, Polar and grant credits, the explore page, the patrons wall \u2014 deployed as one Cloudflare Worker.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bunx wrangler dev",
    "deploy": "bunx wrangler deploy --var DEPLOY_COMMIT:$(git rev-parse --short HEAD)",
    "test": "bun test test/*.test.ts",
    "typecheck": "bunx tsc --noEmit",
    "check": "bun test test/*.test.ts && bunx tsc --noEmit"
  },
  "dependencies": {
    "@open-autonomy/sdk": "workspace:*",
    "@open-autonomy/treasury": "workspace:*",
    "hono": "^4.13.4"
  },
  "devDependencies": {
    "@types/bun": "^1.3.10",
    "typescript": "^5.9.0",
    "wrangler": "4.114.0"
  }
}
