name: CI
on:
  push:
    # Every change lands from a branch (agent/** the agent's, land/** a maintainer's): the push checks the
    # head commit, which is what the main-protected ruleset and auto-merge require. There is no pull_request
    # trigger: the pull request is opened by the landing workflow's bot, and a bot-authored pull request's
    # runs are parked on an approval nobody gives, then expire as a red run under the same check name —
    # which then outranks the green push run and blocks the merge. A fork's change is checked when a
    # maintainer pushes it as a land/** branch.
    branches: [main, 'agent/**', 'land/**']
  workflow_dispatch:

permissions:
  contents: read

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1
      - uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0
        with:
          bun-version: 1.3.10
      - run: bun run check
