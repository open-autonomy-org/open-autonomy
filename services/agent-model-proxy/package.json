{
  "name": "@open-autonomy/treasury",
  "version": "0.1.0",
  "description": "The Open Autonomy treasury (a.k.a. agent-model-proxy): the economy layer — funding-account tree, generic supplier API, bounded model proxy, and the public funding storefront, as one Cloudflare Worker.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bunx wrangler dev",
    "deploy": "bunx wrangler deploy",
    "test": "bun test test/*.test.ts",
    "typecheck": "bunx tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.9.0",
    "wrangler": "4.114.0"
  },
  "dependencies": {
    "hono": "^4.12.27"
  }
}
