{
  "name": "open-autonomy",
  "private": true,
  "description": "A project's agent, checked in, funded by token usage in the open: the template any repository applies, and the platform that meters it and shows the books.",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/open-autonomy-org/open-autonomy.git"
  },
  "type": "module",
  "packageManager": "bun@1.3.10",
  "engines": {
    "node": ">=22.18"
  },
  "scripts": {
    "check": "bun template/apply.ts --check && cd platform && bun install --frozen-lockfile && bun test test/*.test.ts && bunx tsc --noEmit",
    "check:supply-chain": "bun platform/scripts/check-supply-chain.ts",
    "deploy": "cd platform && bun run deploy",
    "check:world": "bun world/run.ts check"
  }
}
