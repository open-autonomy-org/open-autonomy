{
  "name": "@open-autonomy/sdk",
  "version": "3.0.0",
  "description": "The Open Autonomy SDK: the interface an automation implements to be a project, in both directions. The stream client (sessions, the timeline, the agent's setup, the operating state), the timeline codec and drivers, the roster model, the key helpers. Everything it does is a documented HTTP wire any language can speak without it; it depends on nothing.",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/open-autonomy-org/open-autonomy.git",
    "directory": "packages/sdk"
  },
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./roadmap": "./src/roadmap.ts",
    "./client": "./src/client.ts",
    "./drivers": "./src/drivers.ts",
    "./team": "./src/team.ts"
  },
  "files": [
    "src",
    "README.md"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "typecheck": "bunx tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "^1.3.10",
    "typescript": "^5.9.0"
  }
}
