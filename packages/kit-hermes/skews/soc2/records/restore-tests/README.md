# Restore tests

One JSON file per restore of a backup that was tested, added in a reviewed pull request by the person who ran it.
The fields are in `../README.md`. The SOC 2 checklist (C6) expects a record at least each quarter for each durable
store.
