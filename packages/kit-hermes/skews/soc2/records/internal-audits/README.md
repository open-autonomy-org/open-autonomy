# Internal audits

One JSON file per run of the internal audit (the `internal-audit` skill), landed through the ordinary reviewed flow.
Each run records every item of `docs/decisions/SOC2-CHECKLIST.md`, including those that pass. The fields are in
`../README.md`.
