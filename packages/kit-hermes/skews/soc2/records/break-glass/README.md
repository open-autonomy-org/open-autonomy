# Break-glass changes

Every change reaches production through a reviewed pull request and the production environment's approval. If this
project ever allows a change outside that flow, record each one here as `records/README.md` describes, and review it
afterwards. Until a file appears here, the project's position is that no such path is used.
