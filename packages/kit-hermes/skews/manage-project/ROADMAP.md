# __PROJECT__ roadmap

Notable intentions and outstanding outcomes, one `## <id>: <title>` section each, with `Status:`
(planned, active, proposed) and the `Completion:` lines that define done. The PM keeps this file true
once a day; what landed is in `CHANGELOG.md`.
