{
  "name": "@open-autonomy/platform",
  "version": "2.0.0",
  "description": "The Open Autonomy platform: one Cloudflare Worker that holds each project's funds, meters every spend against its account, takes money in, carries the development stream, and serves the funding site and the README widgets.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bunx wrangler dev",
    "deploy": "bunx wrangler deploy",
    "test": "bun test test/*.test.ts",
    "typecheck": "bunx tsc --noEmit",
    "check": "bun test test/*.test.ts && bunx tsc --noEmit"
  },
  "dependencies": {
    "@open-autonomy/sdk": "workspace:*",
    "hono": "^4.13.4"
  },
  "devDependencies": {
    "@types/bun": "^1.3.10",
    "typescript": "^5.9.0",
    "wrangler": "4.114.0"
  }
}
